source "https://rubygems.org"

gem "sage", "~> <%= Sage::VERSION %>"
gem "puma" # Optional, if you want to use Puma instead of Falcon for some reason, but Sage uses Falcon by default. Wait, Sage depends on Falcon.
