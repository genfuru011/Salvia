name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  salvia:
    name: Salvia (View Engine)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby-version: ['3.1', '3.2', '3.3']
        deno-version: ['v2.x']

    steps:
    - uses: actions/checkout@v3

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby-version }}
        bundler-cache: true
        working-directory: packages/salvia

    - name: Set up Deno
      uses: denoland/setup-deno@v1
      with:
        deno-version: ${{ matrix.deno-version }}

    - name: Install dependencies
      working-directory: packages/salvia
      run: |
        bundle install
        # Install Deno dependencies for caching
        deno cache --config assets/scripts/deno.json assets/scripts/sidecar.ts
        deno cache --config assets/scripts/deno.json assets/scripts/build.ts

    - name: Run tests
      working-directory: packages/salvia
      run: bundle exec rake test

  sage:
    name: Sage (Backend Framework)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby-version: ['3.1', '3.2', '3.3']

    steps:
    - uses: actions/checkout@v3

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby-version }}
        bundler-cache: true
        working-directory: packages/sage

    - name: Install dependencies
      working-directory: packages/sage
      run: bundle install

    - name: Run tests
      working-directory: packages/sage
      run: bundle exec rake test
